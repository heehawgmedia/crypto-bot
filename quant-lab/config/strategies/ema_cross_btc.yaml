name: ema_cross_btc_1h
strategy: ema_cross
exchange: kraken
symbol: BTC/USD
timeframe: 1h
params:
  fast: 20
  slow: 50
param_grid:
  fast: [10, 20, 30]
  slow: [50, 100, 200]

# Optional trade rules — applied identically in backtest, walk-forward
# validation, paper, and live. Triggers evaluate on the completed bar;
# exits fill at the next opportunity. After a stop/target exit, re-entry
# needs a fresh signal edge plus the cooldown.
# stop_loss_pct: 5.0
# take_profit_pct: 12.0
# cooldown_bars: 4
